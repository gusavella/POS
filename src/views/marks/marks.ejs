
  
  <%- include('../partials/head')%>
  <body class="body-register">
    <%- include('../partials/header')%>
    <a href="/users/adminPanel"><button class="button-back">Atrás</button></a>
    <main class="main-register">
    <div class="role-box" id="gradient-border">
      <h3 class="console-title">Marcas</h3>
     
      <table description="role table" class="responsive-table">
        <tr class="role-th">
          <th>Nombre</th>
          <th>Operaciones</th>
        </tr>
        
       <% for (mark in marks){ %>
        <tr>
          <form action="/marks/<%=marks[mark].id%>/edit?_method=PUT" method="post">
          <td class="td-input"><input type="text" class="role-input" id="role-input" value="<%=marks[mark].name%>" name="name"></td>
          <td class="role-td">
            <div >
              <button class="new-product-button" type="submit"><i class="fa-regular fa-pen-to-square"></i> Editar</button>
            </div>
          </form>
           <div >
              <form action="/marks/<%=marks[mark].id%>?_method=DELETE" method="post">
                <button class="edit-product-delete-button" type="submit"><i class="fa-solid fa-trash"></i> Borrar</button>
              </form>
           </div>
          </td>
        </tr>
        <%}%>
        <tr>
          <form action="/marks/add" method="post" id="role-form">
            <td class="td-input"><input type="text" name="name" class="role-input-2" id="new-input-role"></td>            
            <td ><button class="new-product-button" type="submit"><i class="fa-solid fa-plus"></i> Agregar</button></td>
            <td></td>
          </form>  
        </tr>
      </table>
      
    </div>
    <p class="warning-msg" id="warning"></p>
    <% if (locals.errors && errors.name){%>
      <div class="warning-msg" id="warning">
          <%= errors.name.msg%>
      </div>
  <%}%>
  </main>
  <%- include('../partials/footer')%>
  <script  src="/js/validationSRCC.js"></script>
</body>

